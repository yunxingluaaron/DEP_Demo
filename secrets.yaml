apiVersion: v1
kind: Secret
metadata:
  name: ab-predictor-secrets
type: Opaque
stringData:
  # Add your secret values here if needed
  # For example:
  # DATABASE_URL: "your-database-url"
  # API_KEY: "your-api-key"
  
  # Flask secret key (generate a random string for production)
  FLASK_SECRET_KEY: "change-this-to-a-random-secret-key-in-production"
  
  # Add other secrets as needed
  # MODEL_API_KEY: "your-model-api-key"
